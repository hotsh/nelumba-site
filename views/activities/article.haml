%article
  %header
    %ul
      %li.title
        #{article.title}
      %li.author
        - if article.author && article.author.avatar
          %img.avatar{:src => article.author.avatar.url}
        - else
          %img.avatar{:src => "/assets/images/avatar.png"}
        %p.name
          = article.author.preferred_display_name if article.author
      %li.date
        #{article.published.strftime("%A, %b %d")}

  #{article.content}
