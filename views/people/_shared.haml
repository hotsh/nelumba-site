%ul.activities
  - if shared.empty?
    %li.message
      None
  - shared.each do |activity|
    - if activity.verb == :post || activity.verb == :share
      %li.activity
        = partial :"activities/list", {:locals => {:activity => activity}}
