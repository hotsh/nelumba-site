%ul
  - people.each do |p|
    %li
      %a{:href => "/people/#{p._id}"}
        #{p.author.short_name}
      %p
        %form{:action => "/people/#{current_person.id}/following", :method => "post", :name => "follow_#{p.author.id}"}
          %input{:name => "author_id", :value => p.author.id, :type => "hidden"}
          %input{:type => "submit", :value => "Follow"}
