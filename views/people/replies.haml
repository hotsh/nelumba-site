.header
  %h1
    Replies to #{person.preferred_display_name}
%ul.activities
  - replies.each do |activity|
    - if activity.verb == :post || activity.verb == :share
      = partial :"activities/show", {:locals => {:activity => activity}}
